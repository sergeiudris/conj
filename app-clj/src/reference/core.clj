(ns reference.core
  (:require [nrepl-server]
            [clojure.repl :refer :all]
            ))


(comment
  
  (nrepl-server/hi)
  
  )

(defn -main []
  (nrepl-server/-main))


;; https://clojure.org/reference

(comment
  
  
  
  
  
  )